import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.colgatemarket.app',
  appName: 'Colgate Marketplace',
  webDir: 'dist'
};

export default config;
